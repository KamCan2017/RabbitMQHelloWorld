namespace Publisher
{
    class Send
    {
        static void Main(string[] args)
        {
             SendHelloWorld.Init();
            //Activate the work queues pattern
            //SendNewTask.Init(args);
        }
    }    
}
